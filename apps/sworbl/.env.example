# Supabase (Settings → API in your project dashboard). Copy to .env — the
# app runs fully local without these; real standings light up with them.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
